DROP TABLE IF EXISTS EMP_2;

CREATE TABLE EMP_2 (
    EMP_NUM      VARCHAR(3)   NOT NULL PRIMARY KEY,
    EMP_LNAME    VARCHAR(15)  NOT NULL,
    EMP_FNAME    VARCHAR(15)  NOT NULL,
    EMP_INITIAL  VARCHAR(1),
    EMP_HIREDATE DATE,
    JOB_CODE VARCHAR(3),
    EMP_PCT DECIMAL(4,2),
    PROJ_NUM VARCHAR(4)

);

-- Insert each row individually into EMP_2
INSERT INTO EMP_2 (EMP_NUM, EMP_LNAME, EMP_FNAME, EMP_INITIAL, EMP_HIREDATE, JOB_CODE, EMP_PCT, PROJ_NUM)
VALUES ('101', 'News', 'John', 'G', '2000-11-08', '502', '5.00', '3.85');

INSERT INTO EMP_2 (EMP_NUM, EMP_LNAME, EMP_FNAME, EMP_INITIAL, EMP_HIREDATE, JOB_CODE, EMP_PCT, PROJ_NUM)
VALUES ('105', 'Johnson', 'Alice', 'K', '1997-02-01', '502','5.00', '3.85');

INSERT INTO EMP_2 (EMP_NUM, EMP_LNAME, EMP_FNAME, EMP_INITIAL, EMP_HIREDATE, JOB_CODE, EMP_PCT, PROJ_NUM)
VALUES ('107', 'Alonzo', 'Maria', 'D', '1997-10-10', '501','5.00', '3.85');

SELECT *
FROM EMP_2;