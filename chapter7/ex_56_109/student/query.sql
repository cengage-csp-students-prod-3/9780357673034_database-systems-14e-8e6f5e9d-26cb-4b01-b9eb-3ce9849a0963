SELECT
    CHECK_NUM,
    BOOK_NUM,
    PAT_ID,
    CHECK_OUT_DATE,
    CHECK_DUE_DATE
FROM
    CHECKOUT
WHERE
    CHECK_IN_DATE IS NULL -- A NULL CHECK_IN_DATE indicates the book has not been returned
ORDER BY
    BOOK_NUM;