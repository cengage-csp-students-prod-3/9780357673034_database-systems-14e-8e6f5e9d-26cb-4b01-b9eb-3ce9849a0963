SELECT
  S.EMP_NUM,
  E.EMP_LNAME,
  E.EMP_FNAME,
  S.SAL_FROM,
  S.SAL_END,
  S.SAL_AMOUNT
FROM
  LGSALARY_HISTORY AS S
JOIN
  LGEMPLOYEE AS E
ON
  S.EMP_NUM = E.EMP_NUM
WHERE
  S.EMP_NUM IN (83731, 83745, 84039)
ORDER BY
  S.EMP_NUM,
  S.SAL_FROM;