SELECT
  P.PROD_CATEGORY,
  COUNT(P.PROD_SKU) AS NUMPRODUCTS
FROM
  LGBRAND AS B
LEFT JOIN
  LGPRODUCT AS P
ON
  B.BRAND_ID = P.BRAND_ID
  WHERE
P.PROD_CATEGORY IS NOT NULL
GROUP BY
P.PROD_CATEGORY
ORDER BY
 P.PROD_CATEGORY;