WITH MINSAL AS (
SELECT
S.EMP_NUM,
MIN(S.SAL_FROM) AS SAL_AMOUNT
FROM
LGSALARY_HISTORY S
GROUP BY
S.EMP_NUM
)

SELECT DISTINCT
E.EMP_NUM,
E.EMP_LNAME,
E.EMP_FNAME,
MINSAL.SAL_AMOUNT AS SAL_AMOUNT
FROM
LGEMPLOYEE E
JOIN 
MINSAL ON MINSAL.EMP_NUM = E.EMP_NUM
ORDER BY
E.EMP_NUM;
