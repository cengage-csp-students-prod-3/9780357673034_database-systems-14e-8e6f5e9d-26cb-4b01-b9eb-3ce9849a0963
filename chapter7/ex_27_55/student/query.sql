SELECT
  D.DEPT_NUM,
  D.DEPT_NAME,
  D.DEPT_PHONE,
  E.EMP_NUM,
  E.EMP_LNAME
FROM
  LGDEPARTMENT AS D
JOIN
  LGEMPLOYEE AS E
ON
  D.EMP_NUM = E.EMP_NUM -- Joining department's manager ID with employee's ID
ORDER BY
  D.DEPT_NAME;