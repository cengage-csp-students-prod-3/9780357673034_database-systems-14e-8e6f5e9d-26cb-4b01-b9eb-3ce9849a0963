SELECT
  E.EMP_NUM,
  E.EMP_LNAME,
  E.EMP_FNAME,
  SUM(I.INV_TOTAL) AS TOTALINVOICES
FROM
  LGEMPLOYEE AS E
JOIN
  LGINVOICE AS I
ON
  E.EMP_NUM = I.EMPLOYEE_ID
GROUP BY
  E.EMP_NUM,
  E.EMP_LNAME,
  E.EMP_FNAME
ORDER BY
  E.EMP_LNAME,
  E.EMP_FNAME;