SELECT DISTINCT
E.EMP_NUM,
E.EMP_FNAME,
E.EMP_LNAME,
MAX(S.SAL_AMOUNT) AS LARGESTSALARY
FROM
LGEMPLOYEE E
JOIN 
LGSALARY_HISTORY S
ON
S.EMP_NUM = E.EMP_NUM
WHERE
E.DEPT_NUM = 200
GROUP BY
E.EMP_NUM,
E.EMP_FNAME,
E.EMP_LNAME
ORDER BY 
LARGESTSALARY DESC, E.EMP_NUM;

